
const person = {
    name: 'Cam',
    age: 22,
    location: {
        city: 'el',
        temp: 92
    }
};

const {name, age} = person;


console.log(`${name} is ${age}.`);

// console.log(`It's ${} in ${}`)
